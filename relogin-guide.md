# 重登 + 拉取洁癖后云端数据 · 逐字操作手册

> 适用场景：已执行 `cleanup_blob_v3.sql`（云端已干净：22 人，唐蓉仅 id=20，测试员已清），
> 现在要让**每一台设备**丢弃本地旧缓存、重新从 Supabase 拉取干净数据。

---

## ⚠️ 操作前必读（3 条）

1. **清缓存会丢掉本机「未同步到云端的本地改动」**。如果某台设备上有还没上传的录入，
   清掉就没了。最稳妥做法：**先在一台主力设备操作并验证**，确认云端是你要的版本后，再处理其他设备。
2. 云端是权威数据源。清缓存 = 本机认云端为准，所以一定是「干净数据赢」。
3. GitHub Pages 部署 v152 通常 1–3 分钟生效；若你看到仍是 v145，多半是旧缓存，按下方步骤清即可。

---

## 方式 A：控制台命令（最快，推荐）

1. 打开系统登录页（或任意已打开的页面标签）。
2. 按 **F12** 打开开发者工具。
   - Mac 用户按 **`Cmd + Option + I`**。
3. 在开发者工具顶部一排标签里，点 **`Console`（控制台）**。
4. 看工具面板**底部**有一个输入框（显示 `>` 或 `◀` 提示符），点进去。
5. 粘贴下面这一整行（可直接复制）：
   ```js
   localStorage.clear(); sessionStorage.clear(); location.reload();
   ```
6. 按 **Enter** 回车。
7. 页面会自动刷新，并跳回**登录页**（因为登录态也被清了）。
8. 用管理员账号登录：`admin@salomon.temp` + 你的密码。
9. 登录成功后，系统启动阶段会自动从 Supabase 拉取云端数据
   （`sync.js` 的 `_pullFromSupabase`），你看到的就是洁癖后的干净数据。

> 这一行同时清了 `localStorage`（Store 缓存）和 `sessionStorage`（登录态），
> 比只清 localStorage 更彻底，避免旧登录态干扰。

### ⚠️ Chrome 粘贴保护拦截了怎么办？

如果粘贴后底部**没有执行**而是出现黄色警告「Please type 'allow pasting' below and press Enter」，
这是 Chrome 的「反粘贴劫持」安全机制在阻止未审查脚本被一键执行。

**解决方法（二选一）**：

- **方法 1（推荐）**：在 Console 输入框里**先输入 `allow pasting`**（不含任何其它字符），按 **Enter**，
  然后**再次粘贴**清缓存命令，按 Enter 执行。
- **方法 2（更稳）**：放弃粘贴，**直接手动输入**命令（逐字敲）：
  ```js
  localStorage.clear(); sessionStorage.clear(); location.reload();
  ```
  手打的代码 Chrome 不会拦截。

---

## 方式 B：图形界面（不想敲代码）

1. 按 **F12**（Mac `Cmd+Option+I`）打开开发者工具。
2. 点顶部 **`Application`（应用）** 标签。
3. 左侧展开 **`Storage` → `Local Storage`** 和 **`Session Storage`**。
4. 选中你的站点域名（类似 `salomon-anfulu.github.io`），**右键 → Clear**，
   或直接点面板顶部的 **`Clear site data`** 按钮（确保勾选了 Local Storage 与 Session Storage）。
5. 关闭开发者工具，按 **F5** 刷新页面。
6. 重新登录。

---

## 验证（每台设备都要做）

| 验证项 | 怎么看 | 正常表现 |
|---|---|---|
| 唐蓉只有 1 条 | 进入「人员管理」页面，搜索「唐蓉」 | 只出现 1 个唐蓉（id=20），**没有**第二个 |
| 测试员已清 | 搜索「测试员」 | **无结果** |
| 陈妍嫔无登录入口 | ①人员管理能看到陈妍嫔（full_time）②退出登录后，用她信息在登录页登录 → 应**登不进去** | 列表有她，但系统无她的登录账号 |
| 版本号已更新 | 看登录页底部 | 显示 **v152**（不再 v145） |

---

## 多设备清单（逐一打勾）

- [ ] 电脑 Chrome
- [ ] 电脑 Safari / Edge（若用过）
- [ ] 手机浏览器（若访问过）
- [ ] 隐身窗口（之前测过的，关掉重开即可，隐身不留缓存）

> ⚠️ **只要有一台旧设备没清**，它下次打开仍带着唐蓉 id=13，一操作就会把脏数据 push 回云端，前功尽弃。
> 所以请确认**所有**访问过的设备都完成上面的步骤。

---

## 兜底：清完还显示旧数据？

说明有旧标签页 / 旧 Service Worker 在喂旧脚本：

1. F12 → **`Application` → `Service Workers`**。
2. 看到 `sw-v15x` 之类 → 点 **`Unregister`**。
3. 回到「方式 A」重来一次。

> 注：v151/v152 改的是**真实代码 bug**（非 SW 缓存问题），普通刷新通常会随缓存更新拿到新代码，
> 大概率不需要清 SW；上面只是兜底手段。

---

## 完成判据

所有设备完成上述步骤 + 验证全部通过 = 迁移彻底收口：
云端干净 + 跨设备实时同步 + 退出登录正常 + 人员数据无脏条目。
