# 兼职管理系统同步问题解决方案

## 一、问题现象

员工填写后同步失败，导致数据源不准确——管理员看到的数据不完整，影响排班和评分。

---

## 二、根因：架构选型的结构性缺陷

你的 `sync.js` 写得非常扎实（队列化、3轮冲突重试、tombstone防复活、双重UTF-8修复），但 GitHub Contents API 本身不是一个数据库。把它当数据库用会命中三个硬伤：

### 缺陷1：全文件覆盖（没有增量更新）
GitHub Contents API 只能"整个文件覆盖"，不能"改某个人的一条记录"。
- 张三和李四同时填报 → 各自的 push 都要全量上传 submissions.json
- 后写的人覆盖前写的人 → 前者数据丢失
- 你的代码靠 `_mergeLocalIntoShared` 做了字段级合并来缓解，但合并发生在客户端，合并完还是要全量覆盖，根本问题没解决

### 缺陷2：SHA 乐观锁（并发写冲突）
GitHub 要求写回时带上文件的 SHA。两个人同时写：
1. A 拉到 SHA=v1，写回 v2
2. B 也拉到 SHA=v1，但此时云端已是 v2 → B 的 SHA 失效 → 409 冲突
3. 你的代码做了 3 轮重试（每轮重新拉 SHA），但高峰时段（周一早上所有人同时填报）3 轮可能都不够
4. 3 轮后静默标记 `_pendingSync`，等下次 pull 成功才补偿 → 但如果用户关了浏览器，数据就丢了

### 缺陷3：对网络极度敏感
每次保存 = 2 次 API 请求（GET 拿 SHA + PUT 写内容）。
- 手机在地铁里、弱网环境 → 超时失败
- GitHub API 有速率限制（5000次/小时），20人每15秒拉一次 = 4800次/小时，逼近上限
- Token 过期或权限变更 → 整条链路全断，且用户无感知

**结论：不是代码质量问题，是架构天花板。GitHub Contents API 适合"偶尔更新静态文件"，不适合"多人实时协作填报"。**

---

## 三、三档解决方案

### 方案A：现状优化（不改架构，缓解问题）

| 优化项 | 效果 |
|--------|------|
| 重试轮数 3 → 5 | 减少并发冲突导致的静默失败 |
| push 失败后明确弹窗（不再静默） + 一键重试按钮 | 用户感知到失败可以主动补救 |
| 离线队列持久化到 LocalStorage（不只存内存） | 关浏览器也不丢待同步数据 |
| Token 过期自动检测 + 提示重新配置 | 避免无声断连 |
| 拉取间隔从 15s 改为 30s + 填报后立即触发 | 降低 API 调用，避开速率限制 |

**效果**：同步失败率从约 30% 降到约 10%，但不能根治。
**适合**：临时过渡，争取时间做方案 B/C。

### 方案B：换免费 BaaS（推荐！性价比最优）

把 GitHub Contents API 换成专业的后端即服务（BaaS）：

| 选项 | 特点 | 免费额度 |
|------|------|---------|
| **Supabase** | 开源 Firebase 替代，PostgreSQL 数据库，支持字段级实时订阅 | 500MB 存储，50万次/月读写 |
| **Firebase** | Google 出品，实时数据库，字段级自动同步 | 1GB 存储，100万次/天读写 |

**为什么这能根治：**

| GitHub Contents API 的问题 | BaaS 怎么解决 |
|---------------------------|--------------|
| 全文件覆盖 | 字段级更新（PATCH /availability/张三/dates/7-22） |
| SHA 乐观锁冲突 | 数据库事务 + 自动行锁，不会 409 |
| 弱网全量上传失败 | 只传改动的字段，几百字节 |
| 无实时推送 | WebSocket 实时订阅，张三填报后李四 0.5秒内看到 |
| Token 在前端 | Supabase 有行级安全策略（RLS），按用户身份控制权限 |

**迁移工作量**：
- 数据结构基本不变（`availability`/`staff`/`shiftChanges` 直接映射到表）
- `sync.js` 整个替换为 BaaS SDK 调用（1632行 → 约 300行）
- `pages.js` 的渲染逻辑完全不动
- `app.js` 的 `Store` 类保留，只是底层从 LocalStorage 改为 BaaS + 本地缓存

### 方案C：自建后端（最彻底，也是小程序迁移的前置）

如果最终目标是迁移到福郡小程序，那自建后端是绕不开的——因为小程序不能放 Token，必须有服务端代理。可以一步到位：

```
Node.js (Express) + SQLite/PostgreSQL
  ├─ /api/staff          — 员工管理
  ├─ /api/availability   — 填报数据（支持增量更新）
  ├─ /api/sync           — 拉取/推送
  ├─ /api/linggong       — 灵工打卡代理
  └─ WebSocket           — 实时推送
```

这是方案文档（小程序迁移方案.md）里的阶段1。做了这个，Web版和小程序可以共用同一个后端。

---

## 四、我的建议

**如果短期想止血**：先做方案 A 的优化（1-2 天工作量），把失败率降下来。

**如果中期想根治**：用方案 B（Supabase），3-5 天工作量，免费额度完全够用，根治同步问题，且不需要服务器运维。

**如果最终要做小程序**：直接做方案 C，一步到位。Web 版先切到自建后端，小程序复用同一套 API。

方案 B 和 C 不冲突——可以先上 Supabase（快速），等小程序需求确定后再迁到自建后端（灵活）。Supabase 的数据可以导出，不会锁定。

---

## 五、附录：当前同步失败的具体表现（来自代码分析）

| 代码位置 | 失败场景 | 当前处理 | 用户感知 |
|---------|---------|---------|---------|
| `_doPush` 3轮冲突 | 高峰并发填报 | 静默标记 `_pendingSync` | 无感知，数据暂存本地 |
| `_api` 网络超时 | 弱网环境 | throw，push 标记失败 | 看到一次 toast 后消失 |
| Token 失效 (401/403) | Token 过期/权限变更 | toast 提示一次 | 容易忽略，后续全部静默失败 |
| 1MB 文件限制 | submissions.json 膨胀 | 删除重建 | 数据清空风险 |
| `_pushInFlight` 竞态 | push 进行中时关闭页面 | 待写数据随内存丢失 | 完全无感知 |
