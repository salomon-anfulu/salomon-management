# 安福路兼职管理系统 — 全面复盘报告

> 生成时间：2026-07-03 | 当前版本：v45 | 代码总量：10,245 行

---

## 一、系统现状概览

### 1.1 代码规模

| 文件 | 行数 | 职责 | 复杂度 |
|------|------|------|--------|
| js/app.js | 2,058 | 数据层 + 路由 + 默认数据 | **75% 是硬编码数据**（L12-L1556） |
| js/pages.js | 5,279 | 全部页面渲染 + 60+ 函数 | 最大文件，评分逻辑 385 行 |
| js/sync.js | 869 | GitHub API 云同步 | 编解码 + 合并逻辑 |
| css/styles.css | 1,322 | 全部样式 | 纯手写，无预处理器 |
| index.html + app.html | 424 | 入口页面 | 双副本维护 |
| login.html | 293 | 登录页 | — |
| **合计** | **10,245** | | |

### 1.2 技术架构特征

```
用户浏览器
  ├── localStorage（本地持久化）
  ├── GitHub Contents API（云端共享存储）
  │     └── data/submissions.json（单一 JSON 文件）
  ├── Chart.js CDN（唯一外部依赖）
  └── 无框架、无构建系统、无 TypeScript
```

---

## 二、核心目标评估

### 🎯 目标 1：多用户端实时更新 — 评估结果：⚠️ 基本可用，但有硬伤

#### 当前方案

| 机制 | 实现 | 间隔 |
|------|------|------|
| Pull（拉取） | 每 60 秒自动拉取 `submissions.json` | 60s |
| Push（推送） | 用户保存数据时触发 PUT | 实时（on save） |
| 冲突处理 | SHA 不匹配时重试（最多 3 次） | — |

#### 核心问题

**问题 1：同步粒度太粗 — 整文件级别**
- 所有数据存在一个 `submissions.json` 中（~200KB）
- 任何一个用户保存任何一条记录，都会推送**整个文件**
- 多人同时编辑时，后推的人覆盖先推的人（Last-Write-Wins）
- **后果**：如果 A 和 B 同时编辑不同记录，后保存的一方会覆盖另一方的修改

**问题 2：合并策略简单粗暴**
```javascript
// sync.js L708 — _mergeArraysById
remote.forEach(item => map.set(item.id, item)); // remote 总是覆盖 local
```
- 只有 `id` 级别的去重，没有字段级合并
- 没有 CRDT（无冲突复制数据类型）或 OT（操作转换）机制
- 同一条记录被两人同时编辑，后推方的版本完全覆盖

**问题 3：staff 合并逻辑重复**
- `_mergeIntoLocal` 中 staff 被合并了两次（L266-289 字段级 + L387-414 整体覆盖）
- 存在冗余计算和潜在不一致风险

**问题 4：60 秒 Pull 间隔有感知延迟**
- 用户 A 刚保存的数据，用户 B 最多需要等 60 秒才能看到
- 对于排班这种需要"看一眼就知道最新状态"的场景，体验不够好

#### 可靠性评分：6/10

- ✅ 基本能工作，编码 Bug 已修复（v40-v45 反复修复 UTF-8 编解码）
- ✅ SHA 冲突重试机制有效
- ⚠️ 并发写入存在数据丢失风险
- ⚠️ 无实时推送（WebSocket/SSE），靠轮询

---

### 🎯 目标 2：减少日常维护成本 — 评估结果：🔴 问题严重

#### 维护成本来源分析

**成本 1：每次数据更新必须改代码（最大痛点）**

目前的数据更新流程：
```
拉取灵工数据 → 手动运行脚本 → 复制 JSON → 粘贴到 app.js defaults 块 → 
改 DATA_VERSION → 改 cache-buster → git push → 验证
```

这意味着：**每周至少 1-2 次代码修改，每次都触及 1500+ 行的 defaults 块**。

| 数据类型 | 更新频率 | 更新方式 | 是否改代码 |
|----------|----------|----------|------------|
| 灵工打卡考勤 | 每周 | 运行脚本 + 手动粘贴 | ✅ 改 app.js |
| 门迎排班 | 每周 | 手动从腾讯文档复制 | ✅ 改 app.js |
| 店务支援 | 每周 | 手动从腾讯文档复制 | ✅ 改 app.js |
| 业绩数据（销售小票）| 每月 | 运行解析脚本 + 手动粘贴 | ✅ 改 app.js |
| 表现评分 | 每次数据更新后 | 手动重算 + 粘贴 | ✅ 改 app.js |
| 人员变动 | 不定期 | 改代码 | ✅ 改 app.js |

**结论：6 种数据类型，全部需要改代码才能更新默认值。**

**成本 2：版本号管理陷阱**

```javascript
// 每次更新必须同步修改 3 处：
const DATA_VERSION = '2026-07-03-v45';  // app.js L1569
defaults._dataVersion = '2026-07-03-v45'; // app.js defaults 块
<script src="js/app.js?v=45"></script>     // index.html + app.html
<script src="js/pages.js?v=45"></script>   // index.html + app.html
```

漏改任何一处 → 用户浏览器加载旧 JS → 数据不一致或白屏。历史上已经踩过多次。

**成本 3：Store.init 合并逻辑复杂（149 行）**

每次版本升级，Store.init 需要处理：
- staff 按 name 合并（新增字段保留、已有字段覆盖）
- ratings 保留用户自定义评分
- 数组类数据做 Mojibake 过滤（v44 新增）
- availability 月份保留
- performanceData 默认覆盖

这段逻辑已经因反复修补变得难以维护，任何修改都有引入新 Bug 的风险。

**成本 4：data/ 目录遗留 19 个临时文件（1.0M）**

```
api_responses.json (111K)     ← 临时调试文件
full_attendance_raw.json (568K) ← 原始数据快照
june_block_v15.js / v18 / v20  ← 历史评分代码片段
ratings_block_v15/v19/v20.js   ← 历史评分代码片段
june_perf_40/42/44.json        ← 历史业绩快照
...
```

这些文件不影响运行，但增加了仓库体积和维护者认知负担。

#### 维护成本评分：3/10

- 🔴 75% 的代码是硬编码数据，每次更新都要改代码
- 🔴 版本号三处同步，极易出错
- 🔴 无数据导入 UI，全靠手动操作
- 🔴 评分联动需要手动触发（虽然有动态计算函数，但默认数据仍是硬编码）

---

### 🎯 目标 3：更直观的体现员工表现 — 评估结果：🟡 功能完整，但展示不够直观

#### 当前评分系统（5 维度）

| 维度 | 权重 | 数据来源 | 计算方式 | 代码行数 |
|------|------|----------|----------|----------|
| 工时支持 | 20% | availability + shiftChanges | 动态计算 | 130 行 |
| 销售业绩 | 20% | performanceData | 动态计算 | 92 行 |
| 行为规范 | 20% | doorSchedule + storeSupport | 动态计算 | 89 行 |
| 考勤纪律 | 20% | linggongAttendance | 动态计算 | 89 行 |
| 顾客好评 | 20% | customerReviews | 动态计算 | 73 行 |
| **合计** | | | | **385 行** |

**好的方面**：
- ✅ 5 维度评分设计合理，覆盖全面
- ✅ 评分函数是动态计算的（不是硬编码结果），数据更新后自动重算
- ✅ 综合分 ≥3.6 → ¥60/h 时薪，有明确的激励导向
- ✅ 有 TOP3 荣誉墙设计
- ✅ 有月份切换器，可看历史

**不够直观的地方**：

**问题 1：评分维度权重均等，无法区分重要性**
- 5 个维度各 20%，但实际管理中"销售业绩"和"考勤纪律"显然比"顾客好评"更重要
- 建议改为加权评分

**问题 2：管理员 vs 员工视角没有区分**
- 目前只有一个"表现评分"页面，管理员看到的全局排名和员工看到的个人数据混在一起
- 员工登录后看到的应该是个人仪表盘（已有 renderPersonalDashboard），但入口不突出

**问题 3：缺少趋势可视化**
- 评分变化趋势（月度对比）没有折线图
- 各维度雷达图没有实现
- 排名变化（上升/下降箭头）没有展示

**问题 4：评分规则不透明**
- 评分阈值（时产≥300→5分、≥240→4分...）硬编码在 pages.js 中
- 员工看不到"我还差多少能到下一档"
- 没有评分说明页面

#### 直观性评分：6/10

- ✅ 评分体系设计完整
- 🟡 缺少趋势图、雷达图等可视化
- 🟡 权重均等不符合实际管理需求
- 🟡 管理员/员工视角未区分

---

## 三、改进方案（按优先级排序）

### P0 — 立即执行（1-2 周）

#### 3.1 清理 data/ 目录遗留文件
```
风险：低 | 收益：减少仓库体积、降低认知负担
操作：git rm data/ 下除 submissions.json 外的所有文件
```

#### 3.2 统一 index.html 和 app.html
```
风险：低 | 收益：消除双副本维护问题
操作：app.html 做软跳转到 index.html，或直接合并为一个入口
```

#### 3.3 评分维度可视化增强
```
风险：低 | 收益：大幅提升员工表现展示直观性
具体：
  - 添加雷达图（Chart.js 已有依赖）：5 维度一眼可见
  - 添加月度趋势折线图：评分变化一目了然
  - 添加排名变化箭头（↑↓→）
  - 评分卡片增加"距下一档还差 X"提示
```

### P1 — 近期执行（2-4 周）

#### 3.4 数据更新流程自动化
```
风险：中 | 收益：大幅降低维护成本
具体：
  - 将 defaults 数据从 app.js 中抽离到独立的 data/defaults.json
  - app.js 通过 fetch 加载默认数据（而非硬编码）
  - 新增"数据导入"页面：管理员可直接粘贴 JSON 或上传文件更新数据
  - 这样每次更新数据不再需要改代码，只需替换 JSON 文件
```

#### 3.5 评分权重可配置化
```
风险：中 | 收益：让评分更符合管理实际
具体：
  - 将评分权重从硬编码改为 Store 中的配置项
  - 管理员可在设置页面调整各维度权重
  - 默认权重建议：销售业绩 30%、考勤纪律 25%、工时支持 20%、行为规范 15%、顾客好评 10%
```

#### 3.6 同步机制优化
```
风险：中 | 收益：减少并发数据丢失风险
具体：
  - 将 Pull 间隔从 60s 缩短到 15-30s
  - _mergeArraysById 改为字段级合并（保留两端的非空字段）
  - 添加"同步状态指示器"（最后同步时间、是否有冲突）
  - 保存前先 Pull 一次，减少覆盖风险
```

### P2 — 中期规划（1-2 月）

#### 3.7 后端化改造（根本解决多用户并发问题）
```
风险：高 | 收益：彻底解决并发写入、实时性、安全性
方案对比：
  A. Supabase（推荐）：免费层够用，PostgreSQL + 实时订阅，无需自己维护服务器
  B. Firebase：Google 生态，实时数据库，免费层有限制
  C. 自建 Node.js + SQLite：最灵活但维护成本最高
推荐方案 A，可渐进迁移：
  1. 先把 staff + ratings 迁移到 Supabase 表
  2. 保留 localStorage 作为离线缓存
  3. 用 Supabase Realtime 替代轮询
```

#### 3.8 模块化重构
```
风险：高 | 收益：代码可维护性质变
具体：
  - 引入 ES Modules（import/export）
  - 拆分 pages.js 为独立页面模块
  - 用 Vite 做构建（开发体验好、生产包小）
  - 逐步引入 TypeScript（可选）
```

---

## 四、合理性评估总结

| 维度 | 评分 | 评价 |
|------|------|------|
| 功能完整性 | 8/10 | 覆盖了排班、考勤、评分、业绩等核心场景 |
| 多用户同步 | 6/10 | 基本可用，但并发写入有数据丢失风险 |
| 维护成本 | 3/10 | 75% 硬编码数据，每次更新必须改代码 |
| 代码可维护性 | 4/10 | 无模块化、无构建、无类型检查 |
| 员工表现展示 | 6/10 | 功能完整但缺少趋势/对比可视化 |
| 技术架构 | 5/10 | 纯前端方案适合快速验证，但不适合长期维护 |

### 核心结论

> **这个系统在过去 6 个月从 0 做到了 v45，功能迭代非常密集（218 个 Task），证明了纯前端方案的快速交付能力。但随着用户增多（多设备同步）和数据量增长（月度积累），当前架构已经接近天花板。**

**最紧迫的三个问题**：
1. 🔴 **数据更新必须改代码** → 把 defaults 抽成 JSON 文件
2. 🟡 **多用户并发写入风险** → 短期优化合并策略，中期迁移 Supabase
3. 🟡 **员工表现展示不够直观** → 添加雷达图 + 趋势图 + 排名变化

---

## 五、改进路线图

```
Phase 1（1-2 周）         Phase 2（2-4 周）          Phase 3（1-2 月）
┌─────────────────┐     ┌─────────────────┐      ┌─────────────────┐
│ ✅ 清理遗留文件  │     │ ✅ 数据文件外置   │      │ ✅ Supabase 迁移 │
│ ✅ 统一入口页面  │     │ ✅ 评分权重可配置  │      │ ✅ 实时订阅同步   │
│ ✅ 雷达图+趋势图 │     │ ✅ 同步策略优化   │      │ ✅ 模块化重构    │
│ ✅ 排名变化箭头  │     │ ✅ 数据导入 UI   │      │ ✅ TypeScript    │
└─────────────────┘     └─────────────────┘      └─────────────────┘
  低风险快速见效          中等风险结构优化           高风险根本性改善
```

---

*报告结束。如需对任何方案展开详细设计或立即执行，请告知。*
