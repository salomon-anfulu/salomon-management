# 安福路 Salomon 兼职管理系统 · 员工更新指引（v155）

> 适用对象：全体兼职同事 ｜ 阅读时长约 3 分钟
> 一句话：请按「**退出登录 → 清掉手机里的旧缓存 → 重新打开登录**」三步操作，即可用上最新版。

---

## 一、这次更新了什么好处（为什么要做这一步）

1. **多人实时同步**：任何人填报 / 修改，其他同事的手机、电脑会**几秒内自动更新**，不再需要手动点「同步」按钮。
2. **账号更安全**：从「全员共用一个口令」升级为**每人独立密码**，首次登录会强制你设置自己的密码。
3. **更干净的状态显示**：之前那个让人困惑的「仅本地」提示已经去掉，连上云端后顶部会显示绿色「已同步」。

⚠️ **为什么必须「清缓存」？**
你的手机 / 浏览器里还存着**旧版本的页面文件**。如果不清理，打开后看到的仍是旧界面（可能还显示「仅本地」），用不上新功能。这一步就是让所有人的设备都加载到最新版（v155）。

---

## 二、核心三步操作

### 第 1 步：在系统里退出登录

1. 打开系统（任意已登录的页面）。
2. 点右上角你的名字 / 头像区域，找到 **「退出登录」** 按钮并点击。
   - 如果已经看不到登录状态（停留在登录页），可跳过这步，直接做第 2 步。

> 退出登录只是断开云端账号，本机旧缓存还在，所以**必须继续第 2 步**。

### 第 2 步：清除本机的网站数据（LocalStorage + 缓存）★最关键

这一步在手机上操作，按你的系统看下方说明。

#### 📱 iPhone / iPad（Safari 浏览器）

1. 先**关闭**本系统页面（回到手机桌面）。
2. 打开 iPhone 的 **「设置」** App。
3. 往下滑，进入 **「Safari 浏览器」**。
4. 继续往下滑，点 **「高级」** → **「网站数据」**。
5. 在顶部搜索框输入 `salomon`，会看到 `salomon-anfulu.github.io` 这一条数据。
6. 向左滑动这一条 → 点 **「删除」**（或点右上角「编辑」后删除）。
7. 返回即可。

> 提示：iOS 没有「应用内一键清缓存」，必须走系统设置，这是最稳妥的方式。

#### 📱 安卓手机（Chrome / Edge 等浏览器）

**方式 A（推荐，只清本系统，最干净）：**

1. 在浏览器里打开本系统页面。
2. 点地址栏左侧的 **🔒 锁形图标**（或「i」信息图标）。
3. 点 **「网站设置」**（Site settings）。
4. 点 **「清除和重置」**（Clear & reset）→ 确认。

**方式 B（通用，但会清掉所有网站记录）：**

1. 点浏览器右上角 **⋮**（三个点）。
2. 点 **「历史记录」** → **「清除浏览数据」**。
3. 时间范围选 **「时间不限 / 全部」**。
4. 勾选 **「Cookie 和网站数据」** 与 **「缓存的图片和文件」**。
5. 点 **「清除数据」**。

> 建议优先用方式 A，只清本系统、不影响其他网站登录状态。

#### 💻 电脑（Chrome / Edge / Safari）

- **Chrome / Edge**：打开本系统页面 → 点地址栏 🔒 →「网站设置」→「清除数据（重置权限）」。
  进阶：按 `F12` → Application → Storage → 勾选全部 → Clear site data。
- **Safari（Mac）**：顶部「Safari」菜单 → 设置 → 隐私 → 管理网站数据 → 搜 `salomon` → 移除。

### 第 3 步：重新打开并登录

1. 在浏览器**重新输入网址**（或用收藏夹重新打开，**不要用**「最近访问」里的旧标签页）：
   ```
   https://salomon-anfulu.github.io/salomon-management/
   ```
2. 页面会自动跳到登录页；如未跳，手动进入 `login.html`。
3. 输入账号密码（见下一节）。

---

## 三、重新登录后的操作

### 1. 登录

- **邮箱**：你的拼音邮箱，格式为 `名字拼音全拼@salomon.temp`
  （例：田佳乐 → `tianjiale@salomon.temp`；不确定自己的邮箱见文末常见问题）。
- **密码**：首次使用初始密码 **`Salomon2026!`**（字母 S 大写，含数字和英文标点 `!`）。

### 2. 首次登录强制修改密码（重要）

登录成功后，系统会**自动弹出「首次登录须修改密码」窗口**，这是新系统的安全升级：

1. **当前密码**：填 `Salomon2026!`。
2. **新密码**：设置你自己的密码，**至少 8 位**（建议字母 + 数字，好记）。
3. **确认新密码**：再输一次，保持一致。
4. 点「确认修改」。
5. 成功后，以后每次登录就用**你刚设的新密码**，不再用共享口令。

> ⚠️ 请务必记住自己的新密码。忘记密码需联系管理员重置。

### 3. 进入系统，确认数据正常

- 登录后进入系统主页（工作台）。
- 顶部状态点应为 **绿色「已同步」**（表示已连上云端实时同步）。
- 看一下「我的填报」「考勤记录」等，确认你的历史数据都在——数据从云端自动拉回。
- 之后正常填报即可；**无需再手动点「同步」按钮**，系统已自动实时同步。

### 4. 日常使用

- 你修改 / 填报的内容会自动同步给所有人。
- 换手机、换浏览器用同一邮箱登录，数据会自动出现。
- 想改密码：点右上角「修改密码」。

---

## 四、常见问题（FAQ）

**Q：我忘记自己的登录邮箱了？**
A：规则是 `名字拼音全拼@salomon.temp`。不确定的，问组长或管理员核对名单。

**Q：提示「邮箱或密码错误」？**
A：先确认邮箱拼写（全小写拼音）；首次登录密码是 `Salomon2026!`（注意大小写）。若已改过密码，用你自己的新密码；忘记则联系管理员。

**Q：清完缓存、登录后，我的历史填报还在吗？**
A：在的。数据存在云端，清的是你手机本地的旧缓存，登录后会自动从云端拉回。

**Q：为什么之前一直显示「仅本地」？**
A：那是旧版的显示误报（基于已废弃的旧同步方案）。现已修复，连上云端即显示「已同步」。

**Q：iOS 找不到「网站数据」在哪里？**
A：路径是 设置 → Safari 浏览器 → 高级 → 网站数据。部分 iOS 版本「高级」在页面最底部。

**Q：不清理缓存直接用旧页面行不行？**
A：不行。旧缓存会让你停留在旧版本，看不到实时同步、也收不到强制改密提示，存在账号安全隐患。

---

*本指引对应系统版本 v155。如在操作中遇到问题，请截图联系管理员。*
