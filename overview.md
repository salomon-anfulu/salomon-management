# v84: 可上班时间冻结

## 改动内容
- **冻结线**: 7月19日（含）之前的可上班时间填报全部冻结，不可修改
- **版本号**: v83 → v84

## 四层冻结防护
1. **日历渲染**: 冻结日期不绑定 onclick，显示 🔒 图标 + 半透明 (opacity:0.6)
2. **弹窗拦截**: `openDateStatusForm()` 直接拦截，显示"该日期已冻结"
3. **保存拦截**: `saveDateStatus()` 二次安全检查
4. **清除拦截**: `clearDateStatus()` 二次安全检查

## 新增代码
- `AVAIL_FREEZE_DATE = '2026-07-19'` 常量
- `_isAvailFrozen(year, mon, day)` 辅助函数
- 图例新增"🔒 19日及之前已冻结"提示
