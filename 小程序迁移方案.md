# 兼职管理系统迁移到「福郡仓库查询小程序」方案

## 一、核心结论

**不是"同步"，是"重建"。** 当前系统是纯浏览器 Web 应用（原生 JS 操作 DOM + LocalStorage + GitHub API），微信小程序是完全不同的运行时，核心代码（5892行 pages.js 渲染逻辑）无法直接移植。

但**业务逻辑可以复用**（评分算法、工时计算、业绩提取等纯函数），预计能节省约 30% 的工作量。

---

## 二、三大不可逾越的技术障碍

### 1. 渲染层（最大工作量）
- **当前**：`pages.js` 5892 行，每个页面用 `content.innerHTML = fn()` 渲染 HTML 字符串
- **小程序**：WXML 模板 + 数据绑定，没有 `innerHTML`、没有 `document.getElementById`
- **影响**：全部 12 个页面的 UI 代码需要用 WXML + WXSS 重写

### 2. 存储层
- **当前**：`localStorage`（通过 Store 封装），含内存缓存
- **小程序**：`wx.setStorageSync` / `wx.getStorageSync`
- **影响**：Store 类需要写适配层，但逻辑（CRUD + cache）可复用

### 3. 安全层（最关键！）
- **当前**：GitHub Token 和 Linggong JWT 直接放在前端，靠 GitHub Pages 部署
- **小程序**：前端代码可被反编译，**Token 放前端等于公开**
- **影响**：**必须自建后端**，Token 藏在服务端，前端只调自己的 API

---

## 三、推荐架构方案

### 技术选型

| 层 | 推荐方案 | 理由 |
|----|---------|------|
| 前端 | 微信小程序原生（WXML/WXSS/JS） | 你已有福郡小程序的框架基础 |
| 后端 | Node.js + Express（或微信云开发） | 复用现有 JS 逻辑，团队学习成本低 |
| 数据库 | 微信云开发数据库（或 MongoDB） | 免运维，直接和微信生态打通 |
| 部署 | 微信云托管（或轻量云服务器） | 微信云开发免域名备案 |

### 数据流对比

```
当前架构：
  浏览器 → localStorage ← GitHub Contents API（token在前端）

迁移后架构：
  小程序前端 → 自建后端 API → 数据库
                          ↘ GitHub API（token在服务端）
                          ↘ 灵工打卡 API（JWT在服务端）
```

---

## 四、四阶段迁移路线

### 阶段1：后端搭建（地基）

**目标**：把当前前端直接调 GitHub/灵工 API 的逻辑，搬到后端

**具体步骤**：
1. 创建 Node.js 项目（Express 框架）
2. 把 `sync.js` 的 GitHub 同步逻辑迁移为后端 API
3. 把 `linggong_ci.js` 的打卡拉取逻辑迁移为后端定时任务
4. 设计 RESTful API 接口：
   - `GET /api/staff` — 获取员工列表
   - `GET /api/availability/:month` — 获取供班数据
   - `POST /api/availability` — 提交个人填报
   - `GET /api/attendance/:month` — 获取考勤
   - `GET /api/performance/:month` — 获取业绩
   - `POST /api/door-schedule` — 门迎排班
   - 等等...
5. Token/Secret 全部放服务端环境变量
6. 部署到云服务器

**可复用的代码**：`sync.js`（GitHub API 调用）、`linggong_ci.js`（打卡 API）、合并仲裁逻辑

### 阶段2：小程序壳应用

**目标**：搭好小程序骨架，微信登录打通

**具体步骤**：
1. 在福郡小程序中新增 tabBar 页面（如"兼职管理"）
2. 对接微信登录（`wx.login` → 后端换取用户身份）
3. 实现角色权限：店长（全权限）vs 兼职（只能看自己的数据）
4. 搭建 API 请求封装层（统一处理 token、错误）
5. 实现本地缓存层（`wx.setStorageSync` 替代 LocalStorage）

**关键设计**：权限模型
- 当前 Web 版没有用户登录概念（所有人用同一个 URL）
- 小程序天然有微信身份，可以做**精细化权限**（兼职只看自己排班、店长看全部）

### 阶段3：核心功能迁移

**目标**：把高频使用的功能搬到小程序

**优先迁移（兼职日常用）**：
1. **个人填报** — 可上班时间填报（最高频）
2. **排班查看** — 门迎排班 / 供班总览（只读展示）
3. **考勤记录** — 灵工打卡数据展示

**可复用的逻辑**：
- 评分算法（5维度计算）
- 工时计算（signIn/signOut 时间差 + 扣午休）
- 名字归一化 `normalizeName()`
- `_esc()` / `_safeSplit()` 等工具函数
- 数据结构定义（staff / availability / performanceData...）

**必须重写的**：
- 全部 UI（WXML 模板）
- 事件绑定（`onclick` → `bindtap`）
- 弹窗（`confirm` → `wx.showModal`）
- 冻结逻辑（保留双拦原则：渲染层 + 操作层）

### 阶段4：进阶功能

**目标**：管理类功能

- 评分系统展示
- 业绩数据管理（含 Excel 导入 → 后端解析）
- 顾客好评管理
- 工作手册
- 数据管理（导入/导出/同步）

---

## 五、可复用 vs 必须重写 详细清单

### 能复用（纯 JS 逻辑，不依赖浏览器 API）

| 模块 | 文件位置 | 复用方式 |
|------|---------|---------|
| 评分算法 | pages.js 评分函数 | 抽成独立模块 |
| 工时计算 | `_calcNetHours` / `_calcPersonMonthHours` | 直接移植 |
| 名字归一化 | `normalizeName()` | 直接移植 |
| 业绩提取算法 | ABBR 映射 + 正则 | 直接移植 |
| XSS 防护 | `_esc()` | 小程序天然防 XSS，可选 |
| 安全工具 | `_safeSplit/_safeReplace` | 直接移植 |
| 数据结构 | Store.defaults | 迁移到后端数据库 schema |
| 合并仲裁 | `_updatedAt` 时间戳逻辑 | 迁移到后端 |

### 必须重写

| 模块 | 原因 |
|------|------|
| 全部 UI 渲染 | innerHTML 在小程序不存在 |
| 路由系统 | 小程序用页面栈，不是 SPA hash 路由 |
| LocalStorage | API 不兼容（但逻辑可复用） |
| GitHub 同步 | 需改为后端 API 调用 |
| 事件绑定 | onclick → bindtap |
| 交互弹窗 | confirm → wx.showModal |
| Service Worker | 小程序有自己的更新机制 |

---

## 六、关键决策点（需你确认）

### 决策1：用微信云开发还是自建服务器？

| | 微信云开发 | 自建服务器 |
|--|-----------|-----------|
| 优势 | 免运维、免备案、和微信深度集成 | 完全可控、无供应商锁定 |
| 劣势 | 绑定微信生态、有调用配额 | 需自己维护、需域名备案 |
| 适合 | 快速上线、团队小 | 长期项目、需要灵活性 |

### 决策2：网页版要不要保留？

- **保留**：网页版继续给店长用（功能全、Excel 导入方便），小程序给兼职用（查看填报排班），两套共用后端数据库
- **废弃**：全部迁移到小程序，网页版下线

### 决策3：迁移范围

- **全量迁移**（12 个模块全搬）：工作量大，但一步到位
- **分批迁移**（先搬 3-4 个核心模块）：快速上线，迭代补充

---

## 七、预估工作量

| 阶段 | 核心产出 | 说明 |
|------|---------|------|
| 阶段1 后端 | API 服务 + 数据库 | 复用 sync.js + linggong_ci.js |
| 阶段2 壳应用 | 小程序框架 + 登录 | 依赖福郡小程序现有架构 |
| 阶段3 核心功能 | 填报 + 推送 + 考勤 | 最高频功能 |
| 阶段4 进阶 | 评分 + 业绩 + 管理 | 管理类功能 |

> 这不是几天能完成的工作，是一个完整的前端重写 + 后端新建项目。建议分批迭代，先上核心功能。

---

## 八、下一步建议

1. **先确认福郡小程序的技术栈**（云开发 or 自建后端、是否已有用户体系）
2. **确定迁移范围**（全量 vs 分批）
3. **确定网页版去留**
4. 如果确认要做，可以从阶段1（后端搭建）开始，这是最没有争议的地基工作
