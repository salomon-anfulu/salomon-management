# 员工登录排错清单（安福路 Salomon 兼职平台）

> 适用场景：员工反馈"账号登录不上 / 提示密码错误"。
> 以 **王靳毓（邮箱 `wangjinyu@salomon.temp`）** 为例，下面步骤对任意兼职通用。

---

## 一、员工自查（先让用户自己确认这 3 点）

1. **邮箱格式对不对？**
   - 正确格式：`姓名拼音@salomon.temp`（全小写，无空格）
   - 王靳毓 → `wangjinyu@salomon.temp`
   - 管理员 → `admin@salomon.temp`
   - ⚠️ 多一个空格、用了中文 @、写成 `.com` 都会登录失败。

2. **密码是不是初始密码 / 改过的密码？**
   - 初始密码（首次登录）：`Salomon2026!`
   - 如果之前改过密码，请用**改后的**密码（不是初始密码）。
   - 注意大小写、全角字符（输入法半角）、数字 0/O、字母 l/1 混淆。

3. **是不是点了「忘记密码」？**
   - 登录页底部有蓝色虚线链接 **「忘记密码？」** → 填邮箱提交。
   - 提交后管理员会收到申请，去后台帮你重置，重置后**用新密码登录**即可。
   - 这是当前最推荐的方式，不依赖邮箱收信。

---

## 二、仍登录不上 → 联系管理员

请员工把**登录邮箱**发给你（管理员），由你在后台重置密码。

---

## 三、管理员重置密码（两种路径）

### 路径 A：Supabase Dashboard（SQL Editor，最稳，推荐）

1. 打开 Supabase Dashboard → 左侧 **SQL Editor**。
2. 粘贴下面语句（把邮箱和新密码换成实际值）并执行：

   ```sql
   UPDATE auth.users
   SET encrypted_password = crypt('新密码', gen_salt('bf'))
   WHERE email = 'wangjinyu@salomon.temp';
   ```

   - 新密码建议形如 `Wang2026!` 这类"姓名缩写+数字+符号"，至少 6 位。
   - `gen_salt('bf')` 是 bcrypt 哈希，和 Supabase 登录用的算法一致，可正常登录。

3. 告诉员工新密码，让他登录后去「设置」里再改成自己记得住的。

### 路径 B：Dashboard 用户列表（新版界面找不到 Edit 按钮时）

> 你之前截图的新版 Supabase 用户详情抽屉里**没有 Edit 按钮**——这是新版 UI 的已知变化，编辑入口不在那个抽屉里。两种办法：

- **办法 1（推荐）**：直接用上面的 **路径 A SQL**，不受 UI 变动影响。
- **办法 2**：在 Authentication → Users 列表里，把鼠标移到对应用户行，行尾会出现 **⋯（三个点）菜单** → 选择 **Edit user / Reset password** → 输入新密码保存。
  - 如果行尾没有 ⋯ 或点开后没有密码字段，说明你的 Dashboard 版本已移除该入口，**回退用路径 A 的 SQL** 即可。

---

## 四、重置后怎么同步到"数据管理"页

- 员工在登录页点「忘记密码」提交后，申请会进入 **Supabase 表 `password_reset_requests`**。
- 你登录系统 → **数据管理** 页 → 最上方「密码重置申请」卡片里能看到待处理列表。
- 你在 Dashboard/SQL 把密码改好后，回到该卡片点 **「标记已处理」**，方便对账。
- 若卡片显示"加载失败"，通常是网络或权限问题，不影响你在 Dashboard 直接重置密码。

---

## 五、常见报错对照

| 报错文案 | 含义 | 处理 |
|---|---|---|
| 邮箱或密码错误 | 密码不对 / 邮箱拼错 | 核对邮箱格式；用「忘记密码」申请重置 |
| 邮箱未验证，请联系管理员 | 账号邮箱状态未确认 | 在 Dashboard Users 里确认该邮箱（或直接用 SQL 改密码，无需验证） |
| 网络未就绪 | 页面没连上 Supabase | 刷新页面重试 |

---

## 六、关于"查密码"

⚠️ **密码无法被"查询"或"查看"**——Supabase 把密码以 bcrypt 哈希存储，任何人都读不到明文（包括管理员和开发者）。所以正确的处理永远是 **"确认账号 + 重置密码"**，而不是"找出原密码"。

---

_本文档可转发给团队。涉及邮箱 / 密码时请通过微信等私聊发送，勿在群聊公开。_
